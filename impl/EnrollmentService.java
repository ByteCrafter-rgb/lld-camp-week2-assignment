public class EnrollmentService {

    Enrollment enroll(Student student, Course course) {
        // Enrollment enroll1 =
        return new Enrollment();
    }

    void completeLesson(Enrollment enrollemnt, Lesson lesson) {
        enrollemnt.status.enrollFlag = true;
    }
}
